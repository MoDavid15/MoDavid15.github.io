// Panel animation and formatting
var panels = (function(){
    panels = {

    };

    panels.init = function(){

        // Get references to DOM objects
        this.list = document.getElementsByClassName("content")[0].children;
    }

    return panels;

})();